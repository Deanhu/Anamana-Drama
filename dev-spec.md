# 漫剧管理后台 — 研发说明 & 关键约束

> 对应 Demo：`demo-v2/drama-detail.html`、`demo-v2/drama-list.html`
> 版本日期：2026-04-23
> 产品负责人：Dean

---

## 1. 素材资源规格

| 资源类型 | 尺寸 | 大小限制 | 格式 | 必填/选填 | 备注 |
|----------|------|----------|------|-----------|------|
| 静态封面 | 516×765px | ≤300KB | 图片 | **必填**（英语） | 每语言独立一张 |
| 静态 Banner | 720×1067px | ≤500KB | 图片 | **必填**（英语） | 每语言独立一张；**⚠️ 规格 2026-04 底可能变更**，需向 UI 确认最新尺寸 |
| Logo | 200×200px | - | PNG | 选填 | 品牌资产，不区分语言 |
| 花絮视频 | - | ≤50MB | MP4 | 选填 | 不区分语言 |

**已砍掉的资源**（当前阶段不做）：
- 动态封面（MP4, 3-5秒）
- 动态 Banner（MP4, 3-5秒）

---

## 2. 多语言配置

### 2.1 基础规则
- 共 **15 个语种**：EN, ZH, JA, KO, ES, FR, TH, PT, DE, AR, HI, ID, VI, TR, RU
- **英语（EN）为必传语言**，其他 14 种不强制
- 多语言覆盖以下内容：

| 内容类型 | 多语言方式 | 英语要求 | 其他语言 |
|----------|-----------|---------|---------|
| 封面 | 每语言独立上传 | 必传 | 选传 |
| Banner | 每语言独立上传 | 必传 | 选传 |
| 简介文本 | 英文手动填写 → 系统自动翻译 → 结果可编辑 | 必填 | 自动生成，可人工修改 |
| 字幕 | 在「剧集管理」中按集×语言矩阵上传 | 必传 | 选传 |

### 2.2 文本自动翻译约束
- **源语言**：英文
- **触发方式**：需提供触发机制（建议「一键翻译」按钮或保存时自动触发，具体由研发评估）
- **翻译结果**：必须展示在后台界面，运营可以直接编辑修改
- **翻译状态**：区分「已翻译」（系统自动翻译完成）和「未翻译」（尚未触发翻译）

### 2.3 素材上传结果展示
运营上传素材后，**必须能看到以下信息**：
- 上传是否成功（成功/失败状态）
- 封面和 Banner 可预览缩略图
- 简介可预览文本内容
- 支持**编辑、修改、重新上传**（覆盖旧版本）

### 2.4 字幕相关约束
- 字幕内容**不需要**在后台编辑（"显示字幕内容没有意义，因为最终发现问题都是在 App 端" —— 需求方原话）
- 只需显示**上传状态**（成功/失败/未上传）
- 字幕文件名需展示（方便运营确认版本）

---

## 3. 视频预览 + 字幕检查（技术待评估）

### 需求描述
需求方希望在后台通过视频播放的方式检查字幕效果：
- 在字幕矩阵中，已上传字幕的单元格可点击「预览」
- 弹出视频播放器，叠加显示对应语言的字幕

### 技术风险
- **后台播放器和 App 播放器的渲染效果可能不一致**（字体、排版、特效）
- 建议研发评估：是否能基于 Web 播放器（如 video.js + WebVTT）实现近似效果
- 如果实现成本过高，可降级为「仅显示字幕文本预览」

---

## 4. 交互约束（通用）

### 4.1 危险操作二次确认
以下操作必须弹出确认框：
- 删除剧集
- 删除字幕文件
- 删除已上传的封面/Banner

### 4.2 批量操作
- 批量添加剧集（支持文件解析自动匹配集数）
- 批量上传字幕（按文件名规则自动匹配集数+语言）

### 4.3 状态一致性
- 多语言内容管理区的完成度摘要需实时反映实际上传状态
- 剧集管理中的字幕矩阵进度条需与实际数据同步

---

## 5. 数据模型建议（供参考）

```
Drama {
  id: string
  title: string (EN, 必填)
  synopsis: string (EN, 必填)
  tags: string[]
  author: string
  owner: string
  language: string (原始语言)
  source: enum
  production_mode: enum
  status: enum (draft / published / offline)

  assets: {
    cover: { [lang]: { url, size, uploaded_at } }     // 每语言独立
    banner: { [lang]: { url, size, uploaded_at } }     // 每语言独立
    logo: { url, size, uploaded_at } | null            // 不分语言
    behind_the_scenes: { url, size, uploaded_at } | null
  }

  translations: {
    [lang]: {
      title: string
      synopsis: string
      source: 'auto' | 'manual'   // 区分机翻和人工
      updated_at: datetime
    }
  }

  episodes: Episode[]
}

Episode {
  ep_number: int
  video: { url, size, duration, uploaded_at } | null
  subtitles: {
    [lang]: { url, filename, size, uploaded_at } | null
  }
}
```

---

## 6. 待定事项

| 项目 | 状态 | 负责人 | 说明 |
|------|------|--------|------|
| Banner 新规格 | 等 UI 确认 | UI 团队 | 2026-04 底 UI 要换规格，当前先用 720×1067 |
| 视频预览可行性 | 等研发评估 | 研发 | 后台播放器 + 字幕叠加的实现方案 |
| 翻译触发机制 | 待产品定义 | Dean | 手动触发 vs 自动触发 vs 混合 |
